All variable declarations (var, let, const) are hoisted, but only var is initialized as undefined.

let and const are in a Temporal Dead Zone (TDZ) until their declaration line is reached, making access before declaration a runtime error.
